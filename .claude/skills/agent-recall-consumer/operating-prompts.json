{
  "schema_version": "1.0",
  "prompts": [
    {
      "id": "adversarial-review",
      "level": 2,
      "template": "Create a project-specific review prompt that treats the current implementation as a first draft. Require at least {{minimum_failure_modes}} distinct plausible failure-mode hypotheses or attack scenarios to be investigated, plus the design assumption most likely to be wrong. Ground the review in the actual changed files, contracts, callers, tests, runtime boundaries, and repository-specific validation already present in recall context. For every attempted failure mode, require an exact trigger scenario and confirming or disproving evidence. A hypothesis disproved by evidence is a successful falsification attempt, not a finding. Do not manufacture defects merely to satisfy the numeric floor; CLEAN remains valid when the required adversarial probes were performed and no evidence-backed defect remains."
    },
    {
      "id": "breaking-change-review",
      "level": 2,
      "template": "Create a project-specific compatibility review prompt from the current task scope and applicable repo-owned project policy in recall context. The generated prompt must determine separately for every affected module, path, package, and public contract whether breaking change is allowed, compatibility is required, or policy is unspecified. Where breaking change is allowed, require the reviewer to evaluate coordinated owner and consumer migration plus deletion of the old path before accepting aliases, adapters, fallbacks, or legacy layers. Where compatibility is required, demand evidence for the external contract and the narrowest migration boundary. Where policy is unspecified or conflicting, return BLOCKED instead of guessing. Require concrete changed files, callers, dependency constraints, release or migration steps, tests, and validation evidence for every conclusion."
    },
    {
      "id": "continue-until-done",
      "level": 1,
      "template": "Continue from the current validated state without restarting discovery. After each step, run the cheapest meaningful validation and use passing evidence as the signal to continue. Do not stop after the first acceptable result. Stop only when {{done_condition}} is satisfied or a concrete blocker makes further safe progress impossible."
    },
    {
      "id": "coverage-mutation",
      "level": 2,
      "template": "Create a project-specific test-hardening prompt. Preserve a hard floor of at least {{minimum_percentage_points}} percentage points coverage growth, but make the generated prompt name the actual files, test entry points, repository commands, existing assertions, and risk-bearing branches found in recall context. Require mutation testing with `{{mutation_command}}`. Coverage alone must not satisfy Done When: the generated prompt must require meaningful mutants to be killed and must report which regression, invariant, edge case, or behavior drift each test protects."
    },
    {
      "id": "deletion-first",
      "level": 2,
      "template": "Create a project-specific simplification prompt that evaluates deletion before extension. Use the current diff, affected symbols, callers, tests, and repository conventions to identify code that can be removed safely. The generated prompt must forbid speculative abstractions, fallbacks, compatibility paths, or configuration unless a verified contract requires them, and it must define evidence proving deletion is safe."
    },
    {
      "id": "evidence-report",
      "level": 1,
      "template": "Do not claim success beyond observable evidence. End with: what changed; why it changed; files touched; exact verification commands run; relevant raw results; verification skipped with reason; remaining risks and unknowns. An unexecuted command, model confidence, or another agent's agreement is not verification."
    },
    {
      "id": "missingness-audit",
      "level": 2,
      "template": "Create a project-specific missingness-audit prompt from the current task and repository context. Make it inspect what should exist but does not yet: tests, validation, error handling, rollback or recovery behavior, observability, documentation, and boundary handling only where the repository evidence or task contract makes them relevant. Require exact file, symbol, workflow, or contract anchors for every proposed gap and forbid invented backlog work."
    },
    {
      "id": "multi-pass-correctness-simplify",
      "level": 2,
      "template": "Create a project-specific three-pass execution prompt. Pass 1 implements only the requested change. Pass 2 audits correctness against the actual task contract, callers, tests, runtime boundaries, and repository validation. Pass 3 simplifies the verified implementation using deletion and deduplication before new abstraction. The generated prompt must name concrete project evidence for each pass and keep correctness before simplification."
    },
    {
      "id": "plan-horizon",
      "level": 2,
      "template": "Create a project-specific planning prompt for the next {{horizon}}, not merely the next step. Use the current repository architecture, open task state, relevant roadmap or TODO evidence, known constraints, dependencies, validation capabilities, and unresolved unknowns from recall context. The generated prompt must produce milestones with observable outcomes, evidence, dependencies, risks, and explicit exit conditions, and must separate committed work from experiments and unknowns."
    },
    {
      "id": "regression-hunt",
      "level": 2,
      "template": "Create a project-specific regression-hunting prompt that investigates at least {{minimum_findings}} concrete high-risk regression hypotheses or search targets using the actual changed production files, existing tests, public contracts, call sites, and high-risk branches from recall context. Require an exact trigger scenario plus confirming or disproving evidence for every probe. When a probe exposes a real regression, broken assumption, missing edge case, weak assertion, behavior drift, or equivalent concrete defect, require the smallest regression test and fix production code unless repository evidence proves the requirement changed. If the bounded probes complete without an evidence-backed defect, Done When must permit an explicit CLEAN result. If material evidence required for the probes is unavailable, permit BLOCKED with the exact gap. Do not manufacture defects to satisfy {{minimum_findings}}; coverage growth alone is not a finding."
    },
    {
      "id": "reproduce-before-fix",
      "level": 2,
      "template": "Create a project-specific bug-fixing prompt that forbids production-code changes until the suspected behavior is reproduced by a failing automated test. Use the actual affected production files, nearest relevant tests, public contracts, callers, runtime boundaries, and repository-specific test and validation commands from recall context. The generated prompt must require the failure to be observed before the fix, then require the smallest production change, rerun the regression test, and run the relevant repository validation. If the bug cannot be reproduced, Done When must permit a BLOCKED result that records the exact attempted reproduction commands, inputs, environment, observed results, and why the bug claim remains unconfirmed. Do not manufacture failure by asserting behavior that repository evidence does not support."
    },
    {
      "id": "reject-and-restart",
      "level": 2,
      "template": "Create a project-specific rejection-and-restart prompt for an implementation that violated the approved contract. Ground every rejection reason in current diff or repository evidence. Preserve valid evidence already gathered, discard the invalid implementation approach, return to the last validated state, and reconstruct the execution prompt whenever the failed assumption changes any L1 contract element: Goal, Context, Constraints, Verification, or Done When. The generated prompt must not negotiate around violations such as scope expansion, public-contract drift, missing reproduction, bypassed verification, or invented architecture. Define the exact evidence required before implementation may resume."
    },
    {
      "id": "retry-stop",
      "level": 1,
      "template": "Retry only while each attempt is based on new evidence. If the same validation fails {{maximum_failures}} times in a row, stop. Do not attempt another speculative fix. Report the exact failing command, the latest concrete error, what changed between attempts, the smallest safe next step, and what information is still missing."
    }
  ]
}
